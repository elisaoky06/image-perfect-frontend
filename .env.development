# In dev, call the API directly so multipart /api/auth/register is not proxied through Vite
# (proxying large multipart bodies often causes 500s). CORS is already enabled on the API.
VITE_API_URL=http://127.0.0.1:5000
